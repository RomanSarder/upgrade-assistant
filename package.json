{
  "name": "upgrade-advisor",
  "private": true,
  "workspaces": [
    "backend",
    "frontend",
    "packages/*",
    "worker"
  ],
  "scripts": {
    "eval": "tsx --tsconfig worker/tsconfig.json eval.ts"
  }
}
