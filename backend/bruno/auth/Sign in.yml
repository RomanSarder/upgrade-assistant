info:
  name: Sign in
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:3000/auth/sign-in
  body:
    type: json
    data: |-
      {
        "email": "roman.sarder.js@gmail.com"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
